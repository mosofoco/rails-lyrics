%h1 Creating artist

= error_messages_for :artist

- form_for(:artist, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
