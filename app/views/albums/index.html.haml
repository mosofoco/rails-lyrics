= render :partial => 'album', :collection => @current_objects

= will_paginate(@current_objects)

- content_for :sidebar do
  %h1 Tools:
  = link_to 'New album', new_object_path 

- content_for :navigation do
  = alphabetical_browser('albums')