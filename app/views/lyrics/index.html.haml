= render :partial => 'lyric', :collection => current_objects

= will_paginate(@current_objects)

- content_for :sidebar do
  = alphabetical_browser('lyrics')

- content_for :navigation do
  = link_to 'New lyric', new_object_path 