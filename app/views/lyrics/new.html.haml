%h1 Creating lyric

= error_messages_for :lyric

- form_for(:lyric, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
